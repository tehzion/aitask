# Handoff — AiTask v2.1.4 (stable) + next actions

Captured: 2026-09-04. Authoritative git/release state verified at capture time.

## Authoritative state
- Branch: `master` — clean, 0 ahead of origin.
- `HEAD` = `origin/master` = `v2.1.4` tag = live production = **`4b66b5b`**.
- Live `/build-info.json`: `v2.1.4 @ 4b66b5b` (channel `production`).
- Production Supabase: up to date through `20260904104435_allow_staff_cycle_published_at`; zero pending migrations; staff-auth triggers enabled; anonymous/secure cutover verified.
- Producer backup + rollback (frontend v2.1.3 @ `6be7569`) recorded under `.release-backups/v2.1.4-2026-09-04/`.

## Verification (all green, ran during audit)
- tsc, eslint, vitest **194/194**
- pgTAP **208/208 PASS** (11 files, local Supabase stack)
- Browser e2e **17/17** + login-recovery-i18n **1/1** + PWA install/offline **1/1**
- `verify:pwa` passed; `verify:supabase` passed (anon denied, secure cutover)

## Test-coverage gaps → recommended next actions (none implemented yet)
- **Tier A — password-setup RPC gap (the ONE client RPC with no DB test):**
  `aitask_complete_password_setup(text,uuid)` has zero pgTAP coverage; e2e only tests the "Link unavailable" dead-end, not the real `/account/password` form (`#new-password` / `#confirm-password` → submit).
- **Tier B — role parity:** `/reports` role-scoped e2e (lift Staff-Account, currently the lightest-covered role) + Client **"Approve delivery" submit** (mirror of the tested request-changes path).
- **Tier C — Approvals lifecycle:** bulk approve/reject, single reject, delete member, add-member full submit, custom-role create/assign/delete.
- **Tier D — create/settings/utility:** Projects create/edit/delete, Calendar create-task-on-date, avatar/sound/password/export, feedback submit, notifications "Load more"/group, command palette.

## Decisions to carry forward
- Local-mode store behavior + DB behavior (pgTAP) are testable now.
- Secure Supabase branches of the above flows are **not** e2e-testable until staging `STAGING_*` secrets are provisioned (staging infra still absent).
- The tagged release gate is the mechanism to enforce new tests in CI; it needs either staging secrets provisioned or another one-time direct-production carve-out.