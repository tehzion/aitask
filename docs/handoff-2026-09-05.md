# Handoff — AiTask v2.1.5 release assurance

Captured: 2026-09-05. Complete the verification fields after the release finishes.

## Release policy

- The canonical Vercel project remains connected to `master`; reviewed merges deploy automatically.
- GitHub pull requests must pass Quality and Security, database rollout validation, and Authenticated Staging QA before merge.
- The matching release tag repeats the full gate and verifies the exact automatically deployed commit.
- Historical direct-production exceptions have been removed from the active workflow.

## Staging state

- Supabase staging project: `dyaxtloducpgjoxuaszk` (`aitask-staging`, Singapore).
- Migration history is aligned through `20260904104435_allow_staff_cycle_published_at`.
- `invite-aitask-member` and `aitask-feedback` are deployed and active.
- `invite-aitask-member` must retain JWT verification and support only the
  persisted `employee`, `supplier`, and `freelancer` worker types. Public
  feedback intentionally remains unauthenticated.
- The Vercel staging project must contain only staging Supabase public variables.
- Fixture service credentials are restricted to seed and cleanup steps and never enter browser code.

## v2.1.5 assurance coverage

- Password completion RPC: authorization, state transition, audit event, version increment, and repeated no-op.
- Hosted password form: validation, real Auth password update, RPC finalization, and subsequent sign-in.
- Account reports: assigned-work visibility with other departments excluded.
- Client approval: submit, decision history, refresh persistence, and no duplicate decision.
- Hosted login/recovery: email-only identifiers, structured failure messaging, and
  recovery enumeration-safe behavior.
- Custom-role protection: protected Boss-only permissions stay hidden and are
  forcibly disabled server-side; `viewReports` is the single role-scoped report
  permission.
- Client Workspace access: assigned Staff require `viewAssignedServiceClients`,
  while Boss/Admin/all-client access remains unchanged.
- Staff task policy: assigned-task edit/delete and creator-owned task management,
  plus `Editor` → `Video Editor` department compatibility normalization.
- Database hardening: retired helper probes fail anonymously, private helper
  schema access is revoked, and pgTAP checks least-privilege grants.
- Existing five-role access, client isolation, notification state, plan save, and interrupted-save retry remain covered.

## Release completion record

- Pull request and required checks: pending.
- Staging deployment and authenticated QA: pending.
- `master` merge commit: pending.
- Tag `v2.1.5`: pending.
- Live `/build-info.json` and route/security verification: pending.
- Production backup/checksum and privilege-only migration: pending.
- Production leaked-password protection: pending; MFA remains intentionally deferred.
- Verified orphan test-account removal: pending; no other business records may change.
- Exact v2.1.5 tag commit and both Edge Function deployments: pending.

## Deferred coverage

- Tier C: registration lifecycle, member deletion/addition, and custom-role lifecycle.
- Tier D: project CRUD, calendar task creation, settings utilities, feedback submission, notification pagination/grouping, and command palette.

## v2.1.5 production-only release gates

Complete these only from the protected release environment with the approved
Supabase and Vercel credentials:

- Repair staging Vercel project/token access, then run staging, source/browser,
  migration replay, pgTAP, advisor, rollout, and anonymous security checks.
- Enable leaked-password protection in production and record the accepted
  password-only Super Admin exception; do not enable MFA as part of this release.
- Back up and checksum production data, verify references, and remove only the
  orphan Staff account with no Auth account or business references.
- Merge through protected `master`, tag the exact merge commit `v2.1.5`, and
  deploy both Edge Functions from that tag.
- Verify production build provenance, public routes/PWA assets, headers,
  Supabase probes, Edge Function JWT settings, password protection, and advisors.
- Do not run authenticated tests against production.
