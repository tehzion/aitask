# Handoff — AiTask v2.2.0 HOD task ownership

Captured: 2026-09-07. This handoff records the v2.2.0 implementation, verified production database alignment, and one-time user-authorized direct-production release state.

## Scope

- Added the protected Staff-based HOD role (`system-hod`) with `manageCreatedTasks`.
- HODs can view and manage tasks they create, including after reassignment, and can work on tasks assigned to them under ordinary Staff restrictions.
- HODs cannot view or change unrelated tasks, move work into a department they do not belong to, or change a task creator.
- Standard Admins retain all-task visibility but can edit only created or assigned tasks. Boss Koo retains unrestricted task control.
- Existing workspaces receive the protected role without automatic member promotion or task/data transformation.

## Database and application evidence

- Migration: `20260907043633_hod_task_ownership_permissions.sql`.
- Backend capability version is now `3`, so older clients enter read-only compatibility mode during the authorization cutover.
- Supabase local replay completed through the HOD migration and the legacy
  privilege hardening migration.
- Local pgTAP passed: 14 files, 256 tests. The full source suite passed: 33 files,
  203 tests; typecheck and lint also passed. These diagnostics used local Node 24
  binaries and are not a substitute for the pinned Node 22 / pnpm 10.4.1 gate.
- The Edge Function now rejects custom roles whose base role does not match the
  invited account and rejects all protected global/account-management permission
  data.

## Release gate

- Package version is prepared as `2.2.0`; the lockfile dependency graph is unchanged.
- The user explicitly waived authenticated staging QA for this one release. Database backup, exact migration dry-run, postflight validation, exact frontend provenance, and rollback controls were retained.
- Production migration history is aligned through `20260907120000_harden_legacy_access_privileges`. The dry run and apply contained exactly the two earlier authorization migrations plus the HOD and legacy-privilege migrations.
- Production preflight/postflight retained 11 members, 189 tasks, 62 projects, 62 clients, 1,069 notifications, 6 registrations, and 22 service entities. The task/project business hash remained `0529533f41898ac5af543dd88cee3476`.
- The only expected record change was the protected HOD preset: custom roles increased from zero to one. No member was automatically assigned HOD.
- Anonymous table and command access probes pass, the legacy anonymous health helper is revoked, backend capability version is `3`, and the production invitation function is active at version 17 with token verification enabled.
- Owner-only logical backups were captured before migration: application schema SHA-256 `3465ae7c9c93cf7aedcbefdfda6f49ff4d8aca38cea46a997eb8b83449b12207` and public data SHA-256 `8757912577b1ac9958704af80b40a592a1984b8ebacf709586474fbe30d0597e`. This is logical-backup evidence, not a Supabase platform-backup claim.
- The previous production frontend artifact is v2.1.4 commit `b7cdc61` at `aitask-8idzr3dld-tehzions-projects.vercel.app`; it remains the frontend rollback target until v2.2.0 live verification completes.

## Deferred or explicitly unchanged

- No role assignment, authenticated production test, public API, or business-record transformation was performed.
- Supabase Auth leaked-password protection remains disabled and should be enabled from the Auth security settings. This is an outstanding security-hardening warning, not an HOD functionality blocker.
- Authenticated five-role staging QA remains waived only for v2.2.0; the normal staging-first gate remains required for later releases.
